// include!("../src/arena/arena.rs");

// pub fn generate_schema(outdir: &OsString) {
//   let schema = schemars::schema_for!(Arena);
//   let schema_file = Path::new(outdir).join("arena-schema.json");
//   fs::write(schema_file, serde_json::to_string_pretty(&schema).unwrap()).unwrap();
// }